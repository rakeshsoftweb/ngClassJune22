import { StrudirctDirective } from './strudirct.directive';

describe('StrudirctDirective', () => {
  it('should create an instance', () => {
    const directive = new StrudirctDirective();
    expect(directive).toBeTruthy();
  });
});
